{
  "name": "hiking-map",
  "version": "1.0.0",
  "description": "Hiking Map with OpenRouteService API integration",
  "main": "index.html",
  "scripts": {
    "start": "node -e \"const http=require('http');const fs=require('fs');const path=require('path');const mime={'.html':'text/html','.js':'application/javascript','.css':'text/css','.json':'application/json','.png':'image/png','.jpg':'image/jpeg','.svg':'image/svg+xml'};http.createServer((req,res)=>{const reqPath=req.url==='/'?'/index.html':req.url.split('?')[0];const filePath=path.join(process.cwd(),reqPath);if(!filePath.startsWith(process.cwd())){res.writeHead(403);return res.end('Forbidden');}fs.readFile(filePath,(err,data)=>{if(err){res.writeHead(404);return res.end('Not found');}const ext=path.extname(filePath);res.writeHead(200,{'Content-Type':mime[ext]||'application/octet-stream'});res.end(data);});}).listen(8765,'0.0.0.0',()=>console.log('Local server running on http://0.0.0.0:8765'));\""
  },
  "keywords": [
    "hiking",
    "map",
    "openrouteservice"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@playwright/test": "^1.60.0"
  }
}
